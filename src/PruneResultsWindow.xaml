<Window x:Class="LudusaviRestic.PruneResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pruning Results" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Name="TitleText" Text="Pruning Results" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Name="SummaryText" Text="Summary information will appear here" TextWrapping="Wrap" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Summary Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Stats Cards -->
            <Border Grid.Row="0" Grid.Column="0" Background="{DynamicResource PanelSeparatorBrush}" Padding="10" Margin="0,0,5,0" CornerRadius="5">
                <StackPanel>
                    <TextBlock Name="SnapshotsDeletedText" Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Snapshots Deleted" FontSize="12" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Background="{DynamicResource PanelSeparatorBrush}" Padding="10" Margin="5,0,5,0" CornerRadius="5">
                <StackPanel>
                    <TextBlock Name="GamesAffectedText" Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Games Affected" FontSize="12" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="0" Grid.Column="2" Background="{DynamicResource PanelSeparatorBrush}" Padding="10" Margin="5,0,5,0" CornerRadius="5">
                <StackPanel>
                    <TextBlock Name="DataDeletedText" Text="0 B" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Data Deleted" FontSize="12" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="0" Grid.Column="3" Background="{DynamicResource PanelSeparatorBrush}" Padding="10" Margin="5,0,0,0" CornerRadius="5">
                <StackPanel>
                    <TextBlock Name="StatusText" Text="Success" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Status" FontSize="12" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Details Tab Control -->
        <TabControl Grid.Row="2" Name="DetailsTabControl">
            <!-- Snapshots Tab -->
            <TabItem Header="Deleted Snapshots">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Filter Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Label Content="Filter by Game:" VerticalAlignment="Center"/>
                        <ComboBox Name="GameFilterComboBox" Width="200" SelectionChanged="GameFilter_SelectionChanged" Margin="5,0,0,0"/>
                        <Button Content="Clear Filter" Click="ClearFilter_Click" Margin="10,0,0,0" Padding="10,5"/>
                    </StackPanel>

                    <!-- Snapshots List -->
                    <DataGrid Grid.Row="1" Name="SnapshotsDataGrid" AutoGenerateColumns="False"
                              IsReadOnly="True" CanUserSortColumns="True" GridLinesVisibility="Horizontal"
                              Background="{DynamicResource ControlBackgroundBrush}"
                              Foreground="{DynamicResource TextBrush}"
                              BorderBrush="{DynamicResource ControlBorderBrush}"
                              AlternationCount="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Snapshot ID" Binding="{Binding ShortId}" Width="100"/>
                            <DataGridTextColumn Header="Game" Binding="{Binding GameName}" Width="200"/>
                            <DataGridTextColumn Header="Date/Time" Binding="{Binding Time, StringFormat='yyyy-MM-dd HH:mm'}" Width="130"/>
                            <DataGridTextColumn Header="Tags" Binding="{Binding TagsString}" Width="*"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="{DynamicResource GridItemBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource GridItemBorderBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="{DynamicResource GridItemAlternateBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource SelectionTextBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Games Summary Tab -->
            <TabItem Header="Games Summary">
                <DataGrid Name="GamesSummaryDataGrid" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserSortColumns="True" GridLinesVisibility="Horizontal"
                          Background="{DynamicResource ControlBackgroundBrush}"
                          Foreground="{DynamicResource TextBrush}"
                          BorderBrush="{DynamicResource ControlBorderBrush}"
                          AlternationCount="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Game Name" Binding="{Binding Key}" Width="*"/>
                        <DataGridTextColumn Header="Snapshots Deleted" Binding="{Binding Value}" Width="150"/>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{DynamicResource GridItemBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource GridItemBorderBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="{DynamicResource GridItemAlternateBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SelectionTextBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </TabItem>

            <!-- Tags Summary Tab -->
            <TabItem Header="Tags Summary">
                <DataGrid Name="TagsSummaryDataGrid" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserSortColumns="True" GridLinesVisibility="Horizontal"
                          Background="{DynamicResource ControlBackgroundBrush}"
                          Foreground="{DynamicResource TextBrush}"
                          BorderBrush="{DynamicResource ControlBorderBrush}"
                          AlternationCount="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tag" Binding="{Binding Key}" Width="*"/>
                        <DataGridTextColumn Header="Occurrences" Binding="{Binding Value}" Width="150"/>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{DynamicResource GridItemBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource GridItemBorderBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="{DynamicResource GridItemAlternateBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SelectionTextBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </TabItem>

            <!-- Raw Output Tab -->
            <TabItem Header="Raw Output">
                <ScrollViewer>
                    <TextBox Name="RawOutputTextBox" IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11"
                             Background="{DynamicResource ControlBackgroundBrush}"
                             Foreground="{DynamicResource TextBrush}" Padding="10"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Close" Click="Close_Click" Padding="15,5" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>
