<UserControl x:Class="LudusaviRestic.OverridesSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" MinWidth="660" MinHeight="420">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{DynamicResource LOCLuduRestOverridesListHeader}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
            <TextBlock Text="  ("/>
            <TextBlock Text="{Binding OverridesCount}"/>
            <TextBlock Text=")"/>
            <Button Content="{DynamicResource LOCLuduRestOverridesClearAll}" Margin="20,0,0,0" Padding="10,4" Click="ClearAll_Click"
                    ToolTip="{DynamicResource LOCLuduRestOverridesClearAllPrompt}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Margin="0,0,0,6">
                <TextBox Width="220" Margin="0,0,8,0" x:Name="SearchBox" TextChanged="SearchBox_TextChanged" ToolTip="Filter"/>
                <Button Content="{DynamicResource LOCLuduRestOverridesRefresh}" Padding="8,4" Click="Refresh_Click"/>
            </DockPanel>
            <DataGrid Grid.Row="1" x:Name="OverridesGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" AlternationCount="2"
                      Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource ControlBorderBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LOCLuduRestGameHeader}" Binding="{Binding GameName}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource LOCLuduRestPerGameBackupOnStopLabel}" Binding="{Binding BackupOnGameStopped}" Width="120"/>
                    <DataGridTextColumn Header="{DynamicResource LOCLuduRestPerGameBackupDuringGameplayLabel}" Binding="{Binding BackupDuringGameplay}" Width="140"/>
                    <DataGridTextColumn Header="{DynamicResource LOCLuduRestPerGameBackupOnUninstallLabel}" Binding="{Binding BackupOnUninstall}" Width="130"/>
                    <DataGridTextColumn Header="{DynamicResource LOCLuduRestPerGameUseCustomRetention}" Binding="{Binding UseCustomRetention}" Width="140"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{DynamicResource GridItemBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource GridItemBorderBrush}"/>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="{DynamicResource GridItemAlternateBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SelectionTextBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{DynamicResource LOCLuduRestOverridesEdit}" Width="90" Margin="0,0,8,0" Click="Edit_Click"/>
            <Button Content="{DynamicResource LOCLuduRestOverridesRemove}" Width="90" Margin="0,0,8,0" Click="Remove_Click"/>
            <Button Content="{DynamicResource LOCLuduRestClose}" Width="90" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
